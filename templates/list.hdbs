{{#each tickets}}
<div class="entry {{status}}">
<i><h4 class="status">{{status}}</h4></i>
<p class="info"><a href="{{url}}" class="url">{{id}} - {{name}}</a></p>
</div>
{{else}}
<p><i>No new tickets!</i></p>
{{/each}}

